#These are the required installations to run the Twitter ETL Apahce application.

sudo apt-get update
sudo apt install python3-pip #installing pip
sudo pip install apache-airflow #installing apache-airflow package
sudo pip install pandas #installing pandas for data manipulations
sudo pip install s3fs
sudo pip install tweepy #tweepy module for accessing the twitter data.
